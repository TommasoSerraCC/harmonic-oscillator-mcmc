# Python dependencies for Harmonic Oscillator analysis
numpy>=1.20.0
matplotlib>=3.4.0
scipy>=1.7.0

# Optional: enables the PNG previews in the GUI's "Risultati" tab and in
# the saved-fits panel. The GUI runs without it, just without thumbnails.
pillow>=9.0.0
