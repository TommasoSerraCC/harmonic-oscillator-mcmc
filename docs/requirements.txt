# Dependencies for building the documentation.
# The runtime packages are needed too, because autodoc imports the
# modules it documents.
#
# sphinx-rtd-theme is used purely as a theme; the docs are published to
# GitHub Pages by .github/workflows/docs.yml, not to Read the Docs.
sphinx>=7.0
sphinx-rtd-theme>=2.0
myst-parser>=2.0

numpy>=1.20.0
matplotlib>=3.4.0
scipy>=1.7.0
